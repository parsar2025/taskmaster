# Directus / Node
node_modules/
npm-debug.log
yarn-error.log
.env
*.log

# Uploads (actual files — don't commit binaries)
uploads/
!uploads/.gitkeep   # optional: keep empty folder in git

# Podman / containers
*.pid
*.sock

# Misc
.DS_Store
Thumbs.db
